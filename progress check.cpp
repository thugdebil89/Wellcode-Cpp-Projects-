#include <iostream>
using namespace std;

int main() {
    int a, b, c, d, e, r;
    cin >> a >> b;
    r = (b - a);
    cout << b + r << " " << (b  +r) + r << " " << ((b  +r) + r) + r;
    return 0;
 }
